name: Keep Server Alive

on:
  schedule:
    - cron: "*/13 * * * *"   # every 13 minutes
  workflow_dispatch:        # allow manual trigger too

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping your server
        run: |
          curl -s https://phibitechbackend.onrender.com > /dev/null
